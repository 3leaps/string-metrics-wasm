#!/bin/sh

# string-metrics-wasm Pre-commit Hook
# Runs fast formatting and quality checks via make precommit

make precommit || {
  echo ""
  echo "❌ Pre-commit checks failed. Common fixes:"
  echo "   - Format issues: 'make format'"
  echo "   - Lint issues: 'make lint-fix'"
  echo "   - Type errors: Fix TypeScript type issues"
  echo "   - Rust issues: 'make rust-fmt && make rust-clippy'"
  exit 1
}
