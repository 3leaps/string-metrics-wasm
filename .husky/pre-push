#!/bin/sh

# string-metrics-wasm Pre-push Hook
# Runs comprehensive validation via make prepush

make prepush || {
  echo ""
  echo "❌ Pre-push validation failed. Common fixes:"
  echo "   - Version mismatch: 'make version-sync'"
  echo "   - License issues: Check 'npm run license:check' output"
  echo "   - Quality checks: 'make quality'"
  echo "   - Build errors: 'make build'"
  echo "   - Test failures: 'make test'"
  exit 1
}
